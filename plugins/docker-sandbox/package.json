{
  "name": "@harness/plugin-docker-sandbox",
  "version": "1.0.0",
  "description": "Docker sandbox plugin — intercepts shell/file tool calls and executes them inside an isolated container",
  "main": "./dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@harness/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}
