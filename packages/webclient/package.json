{
  "name": "@harness/webclient",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone web client for Harness — connects to @harness/server via REST + WebSocket",
  "scripts": {
    "build": "node build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
